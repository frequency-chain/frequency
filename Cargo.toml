[workspace]
members = [
  "node",
  "node/cli",
  "node/service",
  "pallets/*",
	# "xcm/xcm-builder",
	# "xcm/xcm-executor",
	# "xcm/pallet-xcm",
  "common/helpers",
  "xcm-simulator",
  "runtime/*",
]
# exclude = [
#  "xcm-simulator"
# ]

[profile.release]
panic = "unwind"
lto = true
codegen-units = 1
strip = true

# Faster build for bench-dev
[profile.bench-dev]
inherits = "release"
strip = false
codegen-units = 16
lto = false
