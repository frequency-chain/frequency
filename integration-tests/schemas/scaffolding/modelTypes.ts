import { ModelType } from "@frequency-chain/api-augment/interfaces";

// This is an example of bad use of types.
// However, it is the type generated by polkadot TS, so as fas as I can tell
// there is no workaraound.
export const AVRO: 'AvroBinary' | 'Parquet'  = 'AvroBinary';
export const PARQUET: 'AvroBinary' | 'Parquet' = "Parquet";
