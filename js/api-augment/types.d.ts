// Unknown is causing these to be left out of the generated types.
// Adding them here for now.

declare global {
  type SpConsensusSlotsSlotDuration = bigint;
  type CommonPrimitivesMsaDelegatorId = bigint;
  type CommonPrimitivesMsaProviderId = bigint;
}

export {};
